# Target design: the pianist. Opus plays the game, Jev plays the keyboard

Written 2026-09-21 at the user's direction. The user's framing: an RTS is "piano chess"; Opus plays the chess and
needs Jev (TypeSafe's System One model, `docs/harness/jev.md`) to play the piano. The heuristic pile names discrete
macro objectives piecemeal, and every one of them is blind to the others' claims on the same metal, build power and
soldiers; a player has to weigh raids, scouts, army moves, defences and the build order at once. Rulings:

- Opus is the player. It writes prose instructions; Jev reads them every second and picks each unit's next action from
  a menu the code offers. Opus iterates game to game on its instructions instead of on heuristic development.
- The economy planner (`docs/design/2026-09-21-rolling-planner.md`) is not in the loop: Opus dictates the build order
  to Jev. If that fails the fallback is a hybrid with the planner as the economy actuator under Jev's allocation.
- The heuristic brain stays as it is, untouched, as the baseline opponent in batches and as the control. It is not
  the pianist's fallback: when the pianist beats it, it is deleted. Shadow mode over the heuristics' own decision points
  was considered and rejected: it would measure Jev on the framing this design says is wrong.
- Evaluation cost is no objection: the arena runs the shim in lockstep (`WITHIN_REASON_LOCKSTEP`), so a call held on
  the tick pauses the game rather than going stale, and Jev's limit is 1,200 requests a minute (20 a second across all
  parallel games), which at one call per game-second is about today's batch throughput. Opus games are already real
  time. Live GUI games see the call's latency, about 320 ms, which is fine at 1 Hz.

## What is true today
- `crates/bot/src/brain/`: the heuristic brain, `Brain::decide` per tick (10 Hz): the control lane (`micro.rs`: flee,
  fan, focus, kite, on the threat grid) every tick, `think` at 2 Hz: tracking (`track_*`, `survey_spots`, territory,
  wrecks, losses), then the decision heuristics (`protect_commander`, `run_economy` with the planner and the rule
  ladder, `run_army` with contact, raid, scout, waves, squads), then the briefing and the wake conditions for the
  LLM session.
- `crates/bot/src/strategist/`: one `claude -p` session per team beside the brain, an MCP server on 127.0.0.1, modes
  `Strategist` (standing directives) and `Commander` (turns when woken, the game held still meanwhile; tools
  `overview`, `situation`, `map`, `set_directives`, `squad`, `set_production`, `request_turret`, `expansion`,
  `orders`, `wait`, `note`). The prompt is `commander.md` plus `docs/briefs/commander.md`.
- Jev: first contact 2026-09-19 (`experiments/jev/`, `docs/harness/jev.md`): `POST /v1/systemone`, state plus typed
  questions (Choice up to 255 options, Noul, Score up to 10 levels), all answered in parallel over one state,
  probabilities and confidence back, 318-350 ms from this machine, 64k context (32k for the state plus the longest
  question), $0.042 per million input tokens, output free. Documented weaknesses: arithmetic, counting, comparing
  quantities, dates, multi-hop reasoning, distraction by unrelated state. Answers are not bit-stable. It cannot
  choose an option it was not offered. The key lives in `~/.config/within-reason/jev.env` or `TYPESAFE_API_KEY`.
- No HTTP client crate in the workspace (`tiny_http` serves the MCP server); the record format already reserves
  decision records with `source: "jev"` (`docs/harness/record-format.md`).

## The change

### Layers
```
 Opus (claude -p, mode Player)        writes  ─►  the packet: standing instructions in prose, rewritten each turn
        ▲ woken by the wake rules and by Jev's own "this needs the player" answer
 Jev (the pianist, 1 Hz)              reads   ─►  state = packet + rules + the picture; answers one menu per actor
        ▲ the hands: picture writer, menus, actuators, commitment, the record
 engine (commands per tick)                        the control lane (flee, fan, focus, kite) unchanged beneath
```

### The pianist (`crates/bot/src/pianist/`, a mode of the bot: `bot --pianist`, `--pianist --player` with Opus)
Every `PIANIST_FRAMES` (30, one game second; `WITHIN_REASON_JEV_INTERVAL` overrides) the pianist:
1. **Writes the picture** (`picture.rs`): one JSON state. In this order: `instructions` (the packet, or a default
   opening text until Opus writes one), `clock`, `rules` (short standing facts: what wins, what the opponent does,
   what the menu words mean), `economy` (each quantity as a word and a number: "metal 120 of 1000 stored, nearly
   empty; income 12 a second, spending 18: stalling"), `ours` (extractors by spot number, labs, constructors, turrets,
   the army as groups), `enemy` (parties in sight as named places and compositions, remembered buildings, the
   commander if seen, what was lost where lately), `map` (spots by number: ours, theirs, free, exposed, raided;
   named places), `actors` (one entry per actor: what it is, where, what it is doing and since when, its health),
   `recent` (events of the last 30 s). Written by actor, not as one dump, and qualitative first: numbers follow the
   words. Under about 4k tokens; the writer logs its size.
2. **Offers the menus** (`menu.rs`): one Choice per actor that is *free* (idle, its task done or refused, or its
   commitment expired), always with a "continue" or "nothing" option:
   - the commander and each constructor: continue; an extractor at spot N (each free reachable spot among the nearest
     eight); a generator, lab, turret, radar, converter, storage or construction turret near here; assist the nearest
     lab; reclaim the nearest wrecks; repair the nearest damaged unit; walk to a named place; retreat home;
   - each lab: the next unit from its buildable list, or nothing (save the metal);
   - each soldier group: hold; move to a named place; fight to a named place; engage an enemy party in sight; retreat
     home; join another group; and a `split` question (send the nearest N of a type to a place as a new group) when
     the group has more than one type;
   - global Nouls: the base is in danger; a large attack is coming; this needs the player's attention now.
   Destinations and targets are separate Choices consumed only when the action needs them (speculative fan-out):
   `where` over the named places and `whom` over the parties in sight. Named places: metal spots by number, home, the
   enemy start, the grid cells. Two questions per group, one to three per builder, one per lab, three global: 30 to
   100 a second at full size.
3. **Asks** (`crates/jev`): one request. In lockstep the tick waits for the answer (the game is held, as for the
   commander's turns); against a live engine the request runs on a thread and the answer lands next tick, the
   previous decisions standing meanwhile. A 429 waits for `retry-after`; any other failure leaves every actor on its
   current task and is logged; the pianist plays nothing new until the next answer.
4. **Plays** (`hands.rs`, `groups.rs`): each answer becomes orders through the existing actuators: build sites and
   extractor placement (`economy.rs` `build_site_for`, `extractor_site`, `beside_builder`), walking (`routes.rs`),
   the march (`march.rs`), the squad post response for `engage` (as many of the nearest as the odds take, `squads.rs`),
   the factory queue. Groups: new soldiers join the `pool` group at their lab; a group is a set of soldiers with a
   task; `split` makes a new one, `join` merges. Commitment: an actor's task holds until done, refused or its
   time is up (a move until arrival or 20 s; a build until started or refused; a lab unit until queued), and a
   *free* actor switches from "continue" only when the winning option beats it by `SWITCH_MARGIN` (0.15) in
   probability, so wobbling answers do not flip units every second.
5. **Records**: a `d` record per answered question (`source: "jev"`, `kind`: the actor kind, `inputs`: the actor's
   entry and its options, `outputs`: the choice, its probability, the confidence, `latency_ms`), and with
   `WITHIN_REASON_JEV_LOG=1` (the arena sets it) the whole request and response per call to `jev-<ai_id>.jsonl` for
   study. The API key is never logged.

What stays beneath the pianist: the control lane, unchanged; the tracking (`track_*`, `survey_spots`, territory,
wrecks, losses); the briefing and wake rules for Opus; the per-minute status line. What does not run under it: every
decision heuristic in `economy.rs`, `army.rs`, `contact.rs`, `raid.rs`, `scout.rs`, `planner.rs`, `protect_commander`,
`squads.rs`' order taking. The pianist reads no directive.

### The player (strategist mode `Player`; refined 2026-09-21 evening, the user: "Let's work on the player next")
`bot --pianist --player`: a `claude -p` session (Opus) beside the pianist, run as the commander is (lockstep: the game
held still during a turn; forty turns a session, then a fresh one handed the notes and the instructions in force).
- **Its lever is prose.** `instruct { text }` replaces the standing instructions whole; the pianist's next picture
  carries them (the game is held during the turn, so they land the moment it ends). No directive, squad, production
  or turret tool is offered: what the player wants of a unit it says in words and Jev picks the menu option. The
  packet is capped at 8,000 characters (Jev reads it every second beside a picture of about 4k tokens).
- **What it sees.** The turn report opens as the commander's does (score, traded, eco, ground, wrecks, to win, to raid,
  curves, fights, enemies in sight, extractors under threat: the lines that describe the game rather than the
  heuristics), then the hands: every group with its composition, place and task, every builder with its task, every
  lab with its queue, Jev's global judgements when they are high (base in danger, attack coming), and what the hands
  did since the last turn (the `did` lines, capped at forty). `situation` returns the picture Jev is shown, without
  the instructions and rules: the player reads exactly what its hands read. `overview` and `map` as today.
- **Woken** as the commander is (its `wait` settings: a quiet-time maximum, enemies near an extractor, an extractor
  lost, soldiers of a type reaching a count; a group starting an engagement in place of a squad's; the four-minute
  stagnation wake it cannot switch off), and by the hands: when Jev's `needs_player` Noul is at or above 0.8 for
  three calls running, at most once a minute (H-HANDS-PLAYER-WAKE).
- **What the pianist publishes for it** (`Shared.hands`): the last picture, the hands' `did` lines since the last
  turn, the group names engaging, and the global probabilities. The brain also keeps the growth and history tracking
  and publishes the field in pianist mode (they ran only inside the army heuristics before), so the score, curves and
  wake conditions work under the pianist.
- **Not covered:** `--think-penalty` delays directives, field orders and wake settings; the instructions land at once.
  A live-game latency for the player is a later step if the penalty matters here.
- **Read afterwards** with `run/commander_turns.py` (the transcript) and the viewer (the turns as `llm:player`
  decisions beside the pianist's calls). Each game's lessons go into `docs/briefs/player.md`, the knowledge the role
  prompt is followed by, as the commander's brief does.

### Arena
`--pianist` (the bot in pianist mode, heuristic-free, the default packet); `--pianist --player` (Opus on top; the
commander's `--commander-model`, `--effort`, `--claude-config-dir`, `--think-penalty` apply). Both set
`WITHIN_REASON_JEV_LOG=1`. Placement (`--place`) keeps using the build-order search: it is pre-game, not in the loop.
Batches with the pianist run `--parallel 6` at 1 Hz (the request limit).

### Docs
The pianist's mechanical rules (commitment, the switch margin, group formation, menu limits) are heuristics with IDs
`H-HANDS-*` in `docs/heuristics.md`; the question set and the picture's shape are recorded in `docs/harness/jev.md`
with every measured change; each game of the series gets a ledger row; the player's brief is `docs/briefs/player.md`.

### Audit (added 2026-09-21 evening, the user: "give me visibility into what jev is doing throughout the match")
The pianist's log is the audit's source (`docs/harness/record-format.md`, "The pianist's log"): one line per call with
the picture, the questions, the answers, what was played and why a course was kept, and the groups, places and parties
by name. The viewer (`viewer/`) reads it beside the record: every actor of the call at the playhead with its last
decision, the call itself as answer bars with the options as worded and the picture by section, per-minute call
statistics, pianist lanes on the timeline, the names and the groups' movements on the map, and an actor filter on the
decision list. The record's own `d` records (source `jev`) stay the compact form for lanes and the list. Held to the
same tests as the rest of the viewer (`viewer/test/`), with the pianist's panels added to the browser test.

## Steps
1. `crates/jev`: a blocking HTTPS client (a small TLS-capable crate; nothing async), request and answer types,
   the key from the environment or the key file, timeout, 429 with `retry-after`, the versioned model id logged.
   A `jev` binary that sends `experiments/jev/*.json` reproduces the probe.
2. The pianist without Opus: picture, menus, hands, groups, the record. One smoke game against BARb medium at the
   usual settings, read call by call: latency, state size, what it chose, how often a free actor's answer flipped.
   Then 12 games for the ledger against the heuristic bot's numbers.
3. The player: `player.md`, `instruct`, the pianist's log in the turn report, the wake Noul. Games one at a time on
   claude2 as the commander series, each read and integrated into `docs/briefs/player.md`.
4. What the games say decides the next: the picture's shape (whole-game against per-actor), the menu, the interval,
   whether the planner comes back as the economy's hands.

## Judged by
- Step 1: the three probe scenarios answer as on 2026-09-19 (escort 0.72; attack 0.03; return_and_defend 0.93).
- Step 2: the smoke game plays a whole game from a default packet without a heuristic; the flip rate of free actors'
  answers second to second; economy at 5 and 10 minutes against the heuristic batch (planner-2: 9 / 9.5 extractors,
  income 18 / 18) with the same setup (medium, mirror, `--place`, `--corner nw`).
- Step 3: the commander series' measure: games won, minutes survived, extractors by minute 10, and what Opus asked
  for that the menu could not express.

## Status
- 2026-09-21: written; nothing built.
- 2026-09-21, steps 1 and 2 built: `crates/jev` (the probe reproduced: 130-180 ms now, and the answers drifted from
  2026-09-19's, K-jev-answers-drift); the pianist (`brain/pianist/`: picture, menu, hands, groups; `bot --pianist`,
  arena `--pianist` with `jev-<ai>.jsonl`). The user's ruling for the early games: BARb easy first ("if this combo can
  win against a potato"); the arena cannot seat our heuristic bot as the opponent. Smoke games 1-3 against easy
  (ledger): it plays end to end at 20-60 calls a minute, 165 ms median, no failures; what Jev needed was words, not
  numbers (K-jev-words-not-numbers: thirty constructors and no soldier, a six-minute energy stall read as "in balance")
  and one option per kind of action (K-jev-split-vote: six spot options lost to `assist_lab`). Smoke-3, both fixed:
  the best economy yet (14 extractors at 6 min) and a loss at 15 min with 46 soldiers holding at home at 0.8-0.99
  while raids took the map; the odds in words for the army are the next fix, smoke-4.
- 2026-09-21, smoke-4 to smoke-6: the odds in words (the army engages at 0.5-0.9 when it outweighs a party; the
  commander retreats at 0.75-0.9 when outweighed), H-HANDS-REFUSED (a refused spot off the menus), a `scout` action,
  the default packet with named places, and one place question per kind of action with its premise stated
  (K-jev-where-needs-its-premise: a single "where, else home" was answered home on every ask). **Smoke-6 won against
  BARb easy at 19.4 min**, the first win, from the default packet and no decision heuristic. Five games, five findings,
  each a words-or-menu fix, none a strategy rule: the arc's method holds. pianist-easy-1, four games in parallel:
  2-1-1 (two wins as Cortex, a loss and a 40-minute timeout as Armada with 206 soldiers that never went for the base).
  Next: the player (step 3), whose instructions are what the default packet cannot say.
- 2026-09-21 evening, step 3 built: strategist mode `Player` (`player.md`, `docs/briefs/player.md`; tools `instruct`,
  `overview`, `situation` (the hands' picture), `map`, `note`, `wait`, `orders`); the hands published to the session;
  growth tracking and the field under the pianist; `bot --player`, arena `--player`. First game: pianist-player-1.
- 2026-09-21 evening, pianist-player-1: lost at 13.7 min to two harness faults the player could not see through: a
  time-bound instruction re-read every ten seconds made the commander alternate and abandon started builds
  (K-jev-instructions-are-standing, H-HANDS-STARTED), and the decayed frames were reported as losses to an unseen
  enemy (K-hands-abandoned-frames-read-as-losses), so it diagnosed aircraft. Both fixed; pianist-player-2 next.
- 2026-09-21 night, pianist-player-2: lost at 27 min with the best economy and army of the series; the ball of 82
  stood at the edge of the enemy base for five minutes while Jev said advance: pianist groups were committed to no
  turret, so the control lane held them (K-hands-advance-stopped-at-turret-reach). The user, watching: "I think jev is
  failing to attack correctly"; the record says the hands, not Jev. Fixed: `fight_to` is committed to everything.
  pianist-player-3 next.
- 2026-09-21 night, pianist-player-3: **won at 11.1 min**, the player's first win, on the fight_to fix: the ball of 25
  Maces walked through the artillery line to the commander. The user's ruling on the two harness faults: Opus must be
  able to observe the failure mode mid-match "so it can try something else". Built: H-HANDS-STALL (a stalled advance
  in the picture and the report, the control lane's hold on a group named, a wake for the player), refused orders and
  abandoned frames in the "what your hands did" lines.
- **pianist-player-4 (2026-09-21): won at 13.0 min.** The H-HANDS-STALL lines were read and acted on (the player named
  the game-2 shape itself at 11:33). Exposed next: a `fight_to` arrival becomes a hold priced against no turret; `engage`
  is never committed to the commander. Both are the commitment mapping in `micro.rs` (`note_commitments`).

## Footwork per group, and the milling instrument (2026-09-21 night, the user's ruling)

The user: keep the gap between the hands and the game small and predictable; Opus and Jev must be able to take units
and "just order it to do something without our micro engine getting in the way"; some lane rules were never tested
apart, and units "mill about rather than decisively moving towards or away from a threat" because the lane's model
(a point moving at full speed in any direction at once) does not match a unit that turns and accelerates slowly.

- **H-HANDS-LANE.** The player's `lane` tool sets, per group (`group_A`) or for `all`, which of the six footwork rules
  apply: `flee`, `fan`, `focus`, `kite` (the control lane, `micro.rs`), `march` (H-ARMY-MARCH on `fight_to`) and
  `follow` (an engaging group re-sent after its party). `"raw"` is none of them: the group's Fight and Move orders go
  to the engine as the hands gave them. `"on"` is all of them, the default. A list keeps those named. The setting is by
  group name and stands until changed; the group's picture entry shows it when it is not the default, so the report
  carries it. Jev never sets it: it is a standing state the player judges.
- **The milling instrument.** While the lane holds a claim on a unit it sums the path the unit walked, the net
  displacement from where the claim began, and the reversals (a step more than ninety degrees from the last). When the
  claim ends the three go into the journal and the record's next sample as `lane: [claims ended, path, net,
  reversals]`; `run/micro_ledger.py` prints path over net and reversals per claim. Hysteresis in the flee is the first
  candidate fix, to be judged by these numbers in an A/B, not before.
- **The game-4 gaps.** A hold that follows an advance keeps the advance's commitment (`Hold { committed: true }`), so
  the ball that fought into the base keeps fighting there; `engage` is committed to the commander when the group is
  the raid's size for it (`COMMANDER_PARTY_METAL`), as the heuristic answers are.
- The default stays on until a raw arm has beaten it in a batch. Next: pianist-player-5 against BARb **medium**.
- **pianist-player-5 (2026-09-21): lost at 20.3 min, the first game on medium.** The lane tool was used and shown.
  Two harness faults: the base guess pulled to mid-map by a forward extractor of theirs and never corrected
  (H-MAP-ENEMY-CLUSTER tightened, H-MAP-ENEMY-GUESS-EMPTY), and far places never on the menu (H-HANDS-NAMED-PLACES,
  the `mark` tool: the waypoint need the user named). Milling baseline: path over net 1.24, 0.33 reversals per claim.
- **pianist-player-6 (2026-09-21): won at 29.5 min, the first win on medium.** Named far spots and marks were followed;
  the base guess held. The economy was destroyed by Flash raids from 9:00 to 24:00: the hands have no raider answer
  the player found (turrets, a parked Mace ball); the next need is a raider-hunting group the packet can name and
  Pawns in the mix, and a ball's reaction to unseen artillery. Medium so far 1-1.
- 2026-09-22, the user's ruling: "lean on opus and jev to make the actual calls and improve the tools so that opus
  can light things up when needed"; a production whitelist for the player (H-HANDS-PRODUCE, `produce`) as the
  answer to the Mace-only army against medium's raids. The viewer's header links the engine's replay for download.
- **pianist-player-7 (2026-09-22): won at 33.0 min on medium (referee), medium 2-1.** `produce` used from turn one and
  followed exactly. New hands rules from it: unwalkable named spots are not places, a mark off walkable ground says
  so, a forgotten mark ends the walk, an advance stalled 90 s is given up (H-HANDS-STALL). Proposed to the user, not
  built: the damage direction and weapon def through the shim, so the picture can say where unseen shelling comes
  from and the hands can advance on it.

## Shelling from out of sight, and the sea (2026-09-22, the user's rulings)

The user, watching game 6: the army "is getting shelled from just out of frame but has no way to push out to try
and kill it"; and on game 7: the map is an island, the enemy commander was out in the ocean building, "and I
suspect opus never got the information on air units or water behavior. Some units are amphibious, including the
commander."

- **The engine's damage event** carries the weapon's definition and a unit vector from the hit unit toward the
  attacker (its radar-error position) whether or not the attacker is in sight; only the attacker's id is withheld
  (`EngineOutHandler::UnitDamaged`). The interface lists no projectiles. The shim now forwards `from` (the direction)
  and `weapon` (name and range, from `WeaponDef_getName`/`getRange`) on `UnitDamaged`: a protocol change, so
  `run/install_to_bar.sh` before GUI play.
- **H-HANDS-SHELLED.** The brain keeps the last 20 s of hits from unseen attackers as rays (hit position, direction,
  weapon range). The likeliest source is the point nearest all rays when two or more are not parallel, else the point
  four fifths of the range along the one ray. While a shelling is alive the picture has a place `shelling` there
  ("where the unseen {weapon} shelling us is likeliest to stand; its range is {r}"), the group's `under_fire` line
  names the weapon, its range and the compass direction, and a group hit from out of sight for 15 s that is not
  advancing wakes the player (once a minute). The hands can then `fight_to shelling`, or the player can step the
  group out of reach; the choice is Opus's.
- **The sea.** The map tool gains `water`: the share of the map under water and what crosses it: of what we build,
  the commander walks on the sea floor (as the enemy's does), and so do the Crossbow (anti-air only) and the
  Tumbleweed; the Pincer (vehicle plant) and the hovercraft plant's units are the fighting amphibians; our bots and
  vehicles stop at the shore. The picture's and the report's enemy-commander lines say "in the water, where our
  bots cannot walk" when its last seen position is off walkable ground. The brief says the enemy commander retreats
  into the sea when its base is gone and that the referee ends the game when its economy is gone.
- **Not built, reported:** the hands' menu offers only the bot lab and the advanced bot lab; no aircraft plant,
  vehicle plant, hovercraft plant or shipyard, so neither air nor amphibians can be ordered. That is the next lever.
- **pianist-player-8 (2026-09-22): won at 22.3 min on medium, medium 3-1.** The early windmill loop is gone. The
  user: "that's a win -- time for hard!" Built before hard: H-HANDS-SHELLED and the sea in the map tool (above).
- **pianist-player-9 (2026-09-22): won at 11.4 min on HARD, commander killed.** H-HANDS-SHELLED appeared and woke the
  player; weapon names now worded. The cluster refinement tightened to 700 after a third false move of the guess.
- 2026-09-22, the user's rulings: the rules text trimmed to a third ("the hands don't really need to know the
  details"); the enemy-base prediction (H-MAP-ENEMY-CLUSTER) suspended under the pianist, the player marks where to
  go (`mark`, in since game 6). pianist-player-10 was not a game: a hung session and the engine's 60 s watchdog; the
  arena now sets HangTimeout 600 and the driver abandons a turn at 45 s.
- **pianist-player-11 (2026-09-22): won at 9.6 min on hard, hard 2-0.** The first turn hung as in game 10 and the cap
  handled it (45 s of wall time lost, fresh session at once). Cause open: the CLI answers a bare prompt in 3.5 s.
- **pianist-player-12 (2026-09-22): won at 11.1 min on hard, hard 3-0.** No first-turn hang. The `shelling` place
  flickered with each hit and the hands re-sent the ball to it; it now persists 45 s and clears once stood on.
  Next: hard_aggressive (game 13), then the asynchronous Jev call and a realtime arena mode for a human game.

## Realtime (2026-09-22, the user's ruling: "the main requirement for realtime is to disable pausing")

Built for a game against people, and rehearsed headless first:

- `WITHIN_REASON_REALTIME` on the bot (the arena's `--realtime`: speed 1, no engine lockstep). Nothing waits: the
  brain requests a turn without holding (`Shared::request_turn`; a request during a turn is dropped, the next report
  carries the state), the player is told in its first prompt that the game runs on and its orders land when the turn
  ends, and a turn with no answer after 120 s is abandoned and the session replaced (45 s in lockstep).
- The pianist asks Jev on a worker thread. One request in flight at a time, numbered; the answer is played on the
  tick it arrives against the picture and menus it was built from (a call's latency old, 200 ms); an answer that has
  not come after 3 s is dropped and the actors keep their tasks; an answer to a dropped request is discarded. In
  lockstep the call is made in place as before, since at speed 50 a wall-clock wait is ten game seconds.
- Judged by: the record's `late` field (ticks the shim sent late for want of an answer), the pianist's status line
  (calls, failed, dropped, median latency), the transcript's turn wall seconds, and the game itself against hard.
- **pianist-player-13 (2026-09-22): won at 16.0 min against hard_aggressive, commander killed.** Every BARb tier
  beaten by the player over the hands. Next: the realtime rehearsal (`arena --realtime`), then a game against people.

## Raid response (2026-09-22, the user's ruling: "build the detachment-at-a-party option and otherwise rely on opus")

What the hands could do about a raider before the player reacts, from the code: a group is asked at once when a party
comes within 600 of its centre and its entry lists every party at one of our extractors, but the only answer that goes
at a party is `engage`, the whole group; `split` and `scout` take a place; a holding group fights what walks into its
own reach; builders can walk or go home; structures have nothing; the lane never picks a target. realtime-2 showed the
result (K-hands-ball-chases-lone-raiders): the ball after a lone Fav, a lab lost at home.

Decided: one menu row, `send_against` (H-HANDS-DETACH): `how_many` soldiers nearest the party in `whom` become a new
group with an `Engage` task; the rest carry on. Rejected: a standing local rule in the code (nearest few soldiers meet
a raider inside a radius without a pick) and a guard-group mechanism, because the user wants defence to be the
player's, and the player's learnings to carry that responsibility: the prompt now says "Defence is yours" with the
shape of the answer (a detachment of two or four from the nearest group, or a group left where the raids pass; the
ball never chases a lone raider), and the brief carries the lesson from realtime-2. The rules text tells the hands
the row exists in one sentence; the size and the choice are the packet's.

Status 2026-09-22, pianist-player-14 (won on hard, 11.2 min): both starting spots taken for the first time (spot_10 at
1:51, spot_8 at 2:00 on its off-centre site, none abandoned). `send_against` chosen 29 times of 174 offers; the first,
four against a Fav at 4:15, was the intended use, then the cascade: re-asked every 5 s the ball sent four after the
same Flash squad five times in 25 s and the detachments sent detachments, until the player forbade them. Guarded now:
not offered against a party already engaged by a group of ours, nor to a group under four.

## The player's learnings, chat, and the versions (2026-09-22)

**What the player carries between games today.** One thing: `docs/briefs/player.md`, in its system prompt, written by
the harness's author after each game from the player's closing sentences and the record (13 game entries by game
13, 15 KB, about a fifth of the prompt). Its own notes live for one game (they cross session restarts, not games).
The knowledge base and the heuristics ledger are the harness's memory, not the player's. So the learnings are
single-author, one opponent (BARb) on one map (Quicksilver, north-west start), appended in order, never retired, and
never measured for whether a lesson changed play.

**The method proposed (the user: "a clean method for continuing integration"):**
- The brief split into layers the harness assembles per game: the game (units, economy, fights), the opponent
  (BARb by tier; people), the map (Quicksilver; each new map its own file), the scenario (1v1; 2v2 and tier 2). A
  lesson is one line with its source game and a status: held (cites games it held in), contradicted (by which game),
  retired. The assembled brief for a game is the layers that apply, held lessons first.
- The player writes its own debrief. After a match, `run/debrief.py <match>` runs one `claude -p` over the transcript
  and result (no game held, a few thousand tokens) and asks for at most five lines for the next player on this map
  and opponent, and what it wished it could order. The lines land in `docs/briefs/inbox/<match>.md`. Integration is a
  review pass, by the author or a session asked for it: promote to a layer, merge with what is there, mark
  contradictions. The ledger row cites the lessons the game used, from the player's notes.
- Growth is bounded by retirement: a lesson not cited in ten games of its layer is retired to the layer's archive.

**Chat.** The engine's message event now reaches the player: chat lines from people in the game come in the report
("chat since your last turn"), a chat line wakes it (the `wait` flag `chat`, on by default), and the `say` tool
speaks to everyone in the game. The prompt tells it that an experienced player watching is the feedback this
project learns from: answer, say what it is doing, ask what they would do.

**Versions.** The start banner names the commit, the mode, the player's model, effort and Claude Code version, and
the hands' model alias; after Jev's first answer the bot says the versioned model it answered with and its latency.
- 2026-09-22: `run/debrief.py` built and run on pianist-player-13 (+0.0 usage, 40 s). Its "wished" lines, open needs
  for the hands: an order that attack-moves *through* a place rather than to it; a view of where our units have
  had eyes (the spots not looked at lately: `scout.rs` knows), so a sweep is systematic; a new place in the packet
  superseding a group's older destination without a ban by name (H-HANDS-SWITCH's margin keeps the stale task).
- **realtime-1 (2026-09-22): the first unpaused run, timeout at 15 min, lost on the board.** The harness held: ticks
  on time, Jev 255 ms and never dropped, turns 7.5 s median while the game ran. Two faults: a mid-session hang of
  120 s at 2:11 (the sessions' stderr is now kept beside the transcript, `strategist-<ai>-stderr.log`), and the
  `shelling` place flickering under the ball. Correction: the game-12 ledger row said the place "now lives 45 s and is
  dropped once stood on"; that was written but not coded until after realtime-1. It is coded now (`shelling.rs`:
  45 s memory, cleared after 10 s without a hit with a unit of ours within 300 of the place).
- 2026-09-22, the user: "one of its starting mexes is never taken". Two faults found in the records: the picture read a
  spot as taken by its neighbour's extractor within 350 (spot_10, every game; fixed: the extractor within the spot's
  radius takes it), and a started build could be asked over again when the frame was not re-found within 200 of the
  ordered point (spot_8's extractor abandoned for a windmill at 1:22 of game 13; fixed: the task's own started flag
  is the guard). Both to be verified in the next lockstep hard game.
- **realtime-2 (2026-09-22): won on hard at 12.3 min, unpaused.** The harness held again (1 late tick, Jev 265 ms
  median with no drop, 50 turns at 6.4 s median, no hang). The player took a turn every 5 to 10 s through the whole
  middle game (turns 4 to 47 in six minutes): the engage wake fires on every engage pick. The control the user watched
  was poor in one shape: the hands answer a raider with the whole ball (11 of 18 engage picks were 10 to 25 units
  after one Fav, Stump or Beaver, one of them 2,362 away), and nothing answers a raider at a structure when the ball
  is elsewhere (a Fav killed a lab, 2 extractors and 3 windmills; no detachment option takes a party, and a holding
  group fights only what comes within its own reach). See "Raid response" above.

## The first game with people (2026-09-22, human-1)

The user's own client, the AI added from the lobby, the bot started by `run/human_game.sh` (realtime, the player
over the pianist, recording into run/matches). The harness played: the shim found the socket, the banner and the
hands' line were said, 22 turns at 2.5 to 8.5 s of wall time, Jev 239 ms median, six late ticks in five and a half
minutes, no hang. Chat both ways worked and mattered: the opponent's advice was taken at 2:16, the advisor was paid
a poem, and the player asked the table for help when it was lost. Lost to a human Pawn rush at 2:10 against no army.

Fixed from its record: a building beside its builder keeps 150 from any metal spot (the lab over spot_10, the user's
original report in its lobby form); our own chat lines come back as chat events from our host player and are
dropped (the player was woken by its own "gl hf"); `send_against` is offered only against parties of three or
fewer, and a new group is not asked for a review period (two sent against eight Pawns, back a second later).

Open from it: chat names people by number ("player 0"), which reads badly in a lobby; the engine's AI interface
may not carry names (unread). The record has no result line when the host ends the game (the socket closes); the
debrief says "unknown". The player wished for a cheap scout at 1:00 and a lab order that holds a standing count
of a unit rather than "continuously". The economy opening under the player (lab before the second extractor, five
windmills before it) is slower than the heuristic opening was; the brief now says soldiers before the fourth
constructor.

Status 2026-09-22, human-2 (the second lobby game, Cortex, lost at 3:40): the lab stood 98 from spot_10 again (the
anchor cleared 150, the engine moved it 80: a lab now keeps 260); the lobby's boxes put the enemy guess 579 from our
start (discarded for the mirror when nearer than a third of the map's short side; boxes logged); the commander got an
`attack` row for a party beside it that it outweighs (H-HANDS-COMMANDER-FIGHTS); the Jev worker answers only the
newest request when behind (three of 35 dropped in a burst). The brief carries the people's economy advice.

## Texts without a rebuild (2026-09-22, the user: "better tooling for integrating text updates without rebuilding")

The prompts, the briefs and the hands' rules and default instructions were compiled in (`include_str!`), so every
edit to a lesson needed a rebuild, and the build had to precede the edit for it to count (the human-1 round: the brief
was edited after the build and the binary lacked it). Now `crates/bot/src/texts.rs` reads each from the checkout at
every use, found from the binary's own path (or `WITHIN_REASON_TEXTS`), the compiled copy the fallback. The hands'
rules are re-read each call and logged again when they change; a session's prompt is hashed at its start and the
driver replaces the session at the next turn when the prompt on disk differs, handing the notes over as the turn cap
does. So the loop the user wants is: watch the game, edit the brief or the prompt, and the next turn plays it.

Status 2026-09-22, human-3 (the third lobby game, lost at 4:00; the user: "check why it never built units"): the
36cbfd7 fixes held (lab 400 from any spot, the box guess discarded, no drop). No unit because the lab went up before
any generator and emptied the store for 68 s, the hands ordered three constructors in six seconds while told
"constructors 0", the whitelist waited behind the queue, and the lab died two seconds after its first Grunt. Fixed:
one order waiting at most, counts include what is being made, the lab option states its energy draw, the flow words
the seconds to empty; the people's energy advice is in the brief (no rebuild needed for that any more).

Status 2026-09-22, human-4 and the opening (the user: "you need a different opening build order, take a look at the
Matt game"): Matt's first three minutes transcribed by the clock into the brief as the packet to copy
(K-open-matt-order-timings), into the hands' default text, and into the prompt's build-order line; a hands-only
smoke played it from the default text 15 s behind Matt at the lab and 47 s at the first Pawn. Wind and solar are two
options now (the player asked for solar three times in human-4 and got wind). Open: the current wind speed is not in
the picture (the people build solar when the wind happens to be low); it would take a snapshot field and a reinstall.

Status 2026-09-22, human-5 (the user: "better this time, but check Matt's build to see how he handled the commander
assisting early Pawn production"): Matt's commander helps the lab the moment it stands, queues its eight generators
as one batch at 0:53 (built by 1:30, six seconds each), and helps the lab for good from 1:36; the Pawn rate is then
metal-bound (four to fourteen seconds) and the bank is spent to zero. Ours helped from 2:24, built generators one
pick at a time at 8.5 s each, and made a second constructor before the first Pawn. The brief now says so, with the
build-power numbers (K-open-matt-order-timings). A code lever remains open: the hands cannot queue a builder's next
build behind the one in progress (each generator costs an idle pick and a walk; the heuristic path's H-OPEN-QUEUE
does this and the pianist's does not).

## Queue-ahead for builders, and the wind (2026-09-22, the user: "build the queue-ahead for builders, as well as the wind state readout")

H-HANDS-QUEUE: a builder whose started build is 60% done is asked what comes next, with the question saying so, and
the answer goes to the engine queued behind the build and to `Pianist.queued` as the builder's next task; the
frame's finished event (or the next frame's created event, if the first died) promotes it. Helping the lab cannot
be queued, so it is not offered on that ask; a builder that wants to help the lab next is asked again when idle.
The picture shows the queued task as `next`. The wind: `Snapshot.wind` from `Map_getCurWind`, in the report's eco
line ("wind now 5 of this map's 3 to 17") and in the picture's energy words with "weak" under 6. A protocol change:
`run/install_to_bar.sh` before the next lobby game.

Status 2026-09-22, human-7 (the user: "still the wrong build order, but slightly better"): the cadence was Matt's
to the lab (0:36 against 0:33) and the commander helped the lab from 1:14; the lab made three constructors from
"one constructor first" (K-hands-do-not-count-constructors) and the first Grunt came at 2:08. `produce` now takes a
cap after a colon (`corck:1`), counted by the bot on the created event, so the count is the player's lever and not
the hands' judgment; the prompt and the brief say to use it.

Status 2026-09-23, human-8: the opening is Matt's now (lab 0:36, one constructor under `corck:1`, ten Grunts by
2:43); the fight lost it (K-hands-grunts-die-away-from-the-commander). Newcomers walk to the largest group in our
half within 1,500 instead of forming groups of one at the lab; the commander's attack row includes a party within
500 that is busy at our building or fighting our soldiers; the lab's "nothing" words say a choice waits behind the
unit being made (it chose nothing seven times while its constructor was made). The brief carries the user's reading.

## Vehicles and Comet Catcher (2026-09-23, the user: "run a series of games on Comet Catcher with escalating BARb and figure out an alternate meta: a classic vehicles map, different patterns and tempo")

The hands' menu knew one factory, the bot lab, and the picture named only bots. The series needs the plant on the
menu and its units named; the meta itself is the player's to find, game by game, with the brief carrying each game's
lessons into the next.

- Kit: `plant` (`armvp` / `corvp`) and `vehicle_constructor` (`armcv` / `corcv`); `Kit::is_factory` and
  `Kit::is_constructor` replace the lab and constructor tests on the pianist path (the builders' and factories' loops
  of the menu, the picture's actors and counts, the hands' placement, the wake's first turn, the report's counts, the
  medic). The heuristic path keeps `kit.lab`: it is the baseline and is not being taught vehicles.
- Menu: builders get `vehicle_plant` beside `lab`, both priced from the definitions with a line on what each
  factory is for; the construction turret and `assist_lab` go beside / to the nearest finished factory of either
  kind; the factory loop asks a plant as it asks a lab. The actor is `plant_N`; `produce` takes `plant_N` keys.
- Picture: words for every tier-1 vehicle by name (scout car, raider tank, tank, rocket vehicle, artillery,
  amphibious, constructor vehicle), so the plant's options carry a role each as the lab's do.
- Placement: the plant goes to the yard as the lab does (the lab's clearances and gap).
- The brief gets a Comet Catcher section (the strips and their spots, dead wind, BARb opening with a bot lab there in
  91 of 104 recorded games, the vehicle roster with numbers) and says the opening on this map is the player's to
  find. The series: easy, medium, hard, hard_aggressive; `--corner nw` (the west strip), Armada, mirrored, `--place`,
  lockstep at speed 50, `--effort low`; after each game the record is read, the debrief run, and the brief's Comet
  section rewritten before the next tier. That loop is how the meta is found.
