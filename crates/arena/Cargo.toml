[package]
name = "arena"
version = "0.1.0"
edition = "2024"
description = "Batch evaluation: runs headless matches of our bot against BARb and reports win rates"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
