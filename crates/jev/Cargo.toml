[package]
name = "jev"
version = "0.1.0"
edition = "2024"
license = "GPL-2.0-or-later"
description = "Client for TypeSafe's Jev: a state and typed questions in, probabilities out (docs/harness/jev.md)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "3", features = ["json"] }
