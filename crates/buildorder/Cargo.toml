[package]
name = "buildorder"
version = "0.1.0"
edition = "2024"
license = "GPL-2.0-or-later"
description = "Study tool: a crude tier-1 economy simulator and a simulated-annealing build-order search over it"

[dependencies]
serde_json = "1"
